# 人生实验仿真引擎 - 依赖列表
# Life Simulation Engine - Requirements

# Python版本要求: 3.7+

# 核心依赖
# 注：本项目主要使用Python标准库，无需额外依赖

# 可选依赖（用于增强功能）
# 如果需要更丰富的功能，可以取消注释以下包：

# 数据可视化（可选）
# matplotlib>=3.3.0
# plotly>=5.0.0

# 数据处理（可选）
# pandas>=1.2.0
# numpy>=1.19.0

# Web界面（可选）
# flask>=2.0.0
# streamlit>=1.0.0

# 自然语言处理（可选）
# nltk>=3.6
# spacy>=3.0.0

# PDF导出（可选）
# reportlab>=3.6.0
# pypdf2>=2.0.0

# 数据库（可选）
# sqlalchemy>=1.4.0
# sqlite3  # 内置于Python

# 测试（可选）
# pytest>=6.0.0
# pytest-cov>=2.10.0

# 代码质量（可选）
# black>=21.0
# flake8>=3.9.0
# mypy>=0.900

# 说明：
# 本项目设计为轻量级，核心功能仅依赖Python 3.7+标准库
# 所有可选依赖都是为了增强体验，但不是必需的
# 基础使用无需安装任何额外包
